[package]
name = "forge-lsp-proxy"
version = "0.1.0"
edition.workspace = true

[dependencies]
serde_json = "1"
ureq = "2"
zip = { version = "2", default-features = false, features = ["deflate"] }
flate2 = { version = "1", default-features = false, features = ["rust_backend"] }
tar = "0.4"
